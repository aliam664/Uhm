# UHM — وضعیت پروژه و آمادگی برای بازسازی سایت

**وضعیت:** آرشیو محتوای آماده برای بازسازی سایت استاتیک
**آخرین بررسی:** ۱۰ اوت ۲۰۲۶
**منبع حقیقت:** پوشه‌ی [`prompt/`](prompt/README.md)

این فایل برای تصمیم‌گیری سریع پیش از شروع توسعه نوشته شده است. هیچ کد وبی در وضعیت فعلی پروژه نگهداری نمی‌شود؛ این کار عمدی است تا محتوا و مدیا از پیاده‌سازی سایت جدا بمانند.

---

## 1. نتیجهٔ بررسی

| حوزه | وضعیت | توضیح |
|---|---|---|
| هویت برند | آماده | نام UHM، سازنده Ali369، تلگرام و رنگ/تایپوگرافی در `prompt/01-brand/` ثبت شده‌اند. |
| محتوای آموزشی | آماده | ۱۰ بخش مستقل، متن، مسیرهای ویندوز و هشدارها در `prompt/03-content/` موجود است. |
| مدیای اصلی | آماده | لوگو، ویدیوی showcase، فایل RAR و ۱۶ تصویر راهنما/گالری در `assets/` هستند. |
| نقشهٔ اطلاعات | آماده | ترتیب صفحات، ناوبری و جریان کاربر در `prompt/02-structure/` ثبت شده است. |
| مشخصات تجربهٔ کاربر | آماده | RTL، تم، زبان، موبایل، دسترس‌پذیری و رفتارها در `prompt/05-settings/` تعریف شده‌اند. |
| کد سایت | عمداً موجود نیست | هنگام تأیید ساخت، HTML/CSS/JS استاتیک از نو ایجاد می‌شود. |

---

## 2. ساختار استاندارد فعلی

```text
Uhm/
├── README.md                  # نمای کلی محصول و لینک‌های کلیدی
├── PROJECT-AUDIT.md           # این گزارش و تصمیم‌های پیش از توسعه
├── content/                   # ۸ متن خام فارسی؛ مرجع اولیه، نه محل نمایش مستقیم
├── assets/
│   ├── download/              # uhm-graphics-pack.rar
│   ├── guides/                # تصاویر آموزش و گالری، دسته‌بندی‌شده بر اساس موضوع
│   ├── media/                 # ویدیوی showcase
│   └── logo.png
└── prompt/                    # منبع حقیقت محصول و طراحی
    ├── 01-brand/              # برند
    ├── 02-structure/          # ساختار اطلاعات و صفحات
    ├── 03-content/            # محتوای تاییدشدهٔ ۱۰ راهنما
    ├── 04-media/              # نگاشت مدیا و کپشن‌ها
    ├── 05-settings/           # مشخصات فنی و رفتار UI
    └── 06-redesign/           # محدودیت‌ها و بریف توسعه
```

### قانون نگهداری

- فایل‌های `content/` فقط متن خام منبع‌اند. نسخهٔ ساخت‌یافته و قابل‌نمایش هر آموزش در `prompt/03-content/` قرار دارد.
- قبل از تغییر نام یا جابه‌جایی هر مدیا، `prompt/04-media/MEDIA-MAP.md` و در صورت نیاز کپشن‌ها به‌روزرسانی شوند.
- فایل RAR باید با همان فرمت RAR باقی بماند، مگر آن‌که فایل دانلودی واقعاً جایگزین شود.
- در آغاز توسعه، پوشه‌های `css/`، `js/`، `pages/` و `index.html` ایجاد می‌شوند؛ تا آن زمان نباید نسخه‌های ناقص یا تکراری کد به آرشیو افزوده شوند.

---

## 3. پوشش مدیا و شکاف‌ها

| راهنما | مدیای موجود | تصمیم UI در ساخت سایت |
|---|---|---|
| نصب Pure | ویدیوی CDN | بج «ویدیو»؛ ویدیوی لوکال در پروژه موجود نیست و اختیاری است. |
| CSP | ۱ عکس | بج «با عکس». |
| PP Filter | بدون عکس | بدون بج؛ متن راهنما کافی است. |
| Pure Config | ۲ عکس | بج «با عکس». |
| Video Setting | ۱ عکس | بج «با عکس». |
| Chaser Camera | ۳ عکس | بج «با عکس». |
| HUD | ۱ عکس | بج «با عکس». |
| SPR Light | بدون عکس | بدون بج؛ متن راهنما کافی است. |
| Neck FX | ۱ عکس + لینک خارجی | بج «با عکس» و نگه‌داری لینک acstuff. |
| تست گرافیک | ۹ عکس | گالری با lightbox. |

**نتیجه:** هیچ فایل ضروری برای شروع طراحی و توسعهٔ سایت کم نیست. تنها کمبودهای اختیاری، تصویر برای PP Filter و SPR Light و نسخهٔ لوکال ویدیوی آموزش Pure هستند.

### کنترل یکپارچگی انجام‌شده

- امضای فایل‌های اصلی بررسی شد: `logo.png` یک PNG معتبر، `uhm-showcase.mp4` یک MP4 معتبر و `uhm-graphics-pack.rar` یک آرشیو RAR 5 است.
- لوگو `640×640` است؛ تصاویر آموزشی و گالری نیز همگی قابل‌خواندن‌اند و ابعاد آن‌ها بررسی شده است.
- موجودی آرشیو: **۲۸** سند طراحی/محتوا، **۱۹** تصویر، **۱** ویدیوی محلی و **۸** متن خام راهنما.
- ابزارهای بررسی عمیق MP4/RAR در محیط حاضر موجود نبودند؛ در مرحلهٔ انتشار سایت، یک‌بار پخش کامل ویدیو و استخراج آزمایشی RAR روی سیستم مقصد انجام شود.

---

## 4. قراردادهای قطعی محصول

این موارد هنگام ساخت سایت نباید تغییر کنند:

1. محصول: پک گرافیکی UHM برای **Assetto Corsa**، نسخه `v1.0`.
2. رابط پیش‌فرض: فارسی و راست‌به‌چپ؛ سوییچ انگلیسی و تم روشن/تیره در نظر گرفته شود.
3. شخصیت بصری: پس‌زمینهٔ تیره و آبی الکتریکی برند؛ از رنگ اصلی متفاوت استفاده نشود.
4. سازنده و پشتیبانی: **Ali369** و `@uhm_009` باید در بخش‌های کلیدی سایت قابل مشاهده باشند.
5. دانلود: `assets/download/uhm-graphics-pack.rar`.
6. ویدیوی نمایش: `assets/media/uhm-showcase.mp4`؛ پس‌زمینهٔ هیرو می‌تواند blur محدود داشته باشد اما پلیر اصلی باید واضح باشد.
7. راهنمای «نصب Pure» و «Pure Config» دو مرحلهٔ جدا هستند و نباید با هم ادغام شوند.
8. مسیرهای ویندوز باید قابل‌کپی و در نمایش LTR باشند.
9. یادآوری بکاپ، پیش‌نیازها و هشدارهای خطر باید حفظ شوند.

---

## 5. ترتیب پیشنهادی آغاز توسعه

پس از تأیید شما برای ساخت سایت:

1. محتوای `prompt/` را به‌عنوان قرارداد محصول نهایی مرور می‌کنم.
2. نقشهٔ صفحهٔ اصلی و ۱۰ صفحهٔ راهنما را بر مبنای `prompt/02-structure/` می‌سازم.
3. ابتدا سیستم پایه: توکن‌های طراحی، RTL/LTR، تم، هدر/منو و موبایل‌فرست (mobile-first).
4. سپس صفحهٔ اصلی، دانلود، ویدیو، کارت‌های راهنما و چک‌لیست پیش‌نیازها را پیاده‌سازی می‌کنم.
5. ده راهنما، مدیا، گالری و لینک‌های خارجی را اضافه می‌کنم.
6. در پایان عملکرد، دسترس‌پذیری، نمایش موبایل، دانلود، ویدیو و مسیرهای ناوبری را تست می‌کنم.

---

## 6. چک‌لیست تصمیم پیش از ساخت

- [x] محتوای اصلی مشخص است.
- [x] مدیا نام‌گذاری و دسته‌بندی شده است.
- [x] فایل دانلود و هویت برند موجودند.
- [x] ساختار و رفتار هدف سایت مستند شده است.
- [x] کد قدیمی عمداً از آرشیو کنار گذاشته شده است.
- [ ] تأیید کاربر برای شروع ساخت سایت.

برای نقشهٔ کامل، از [`prompt/README.md`](prompt/README.md) شروع کنید.

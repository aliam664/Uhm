# گزارش بررسی و باگ‌های پروژه UHM

بررسی کامل روی سورس، اجرای صفحه‌ها در یک مرورگر آزمایشگاهی (jsdom + سرور واقعی HTTP) و بررسی همه مسیرها و استایل‌ها انجام شد. خلاصه: **ساختار و محتوا سالم است، فایل‌ها همگی سر جایشان هستند و هیچ خطای «مرگبار» که سایت را از کار بیندازد وجود ندارد — اما چند باگ واقعی و چند مورد کد مرده / ناسازگاری وجود دارد.**

> **وضعیت رفع (اخرین به‌روزرسانی):** باگ‌های ۱، ۲، ۳، ۴، ۵، ۶ و ۷ **رفع و تست شدند** و برای ریسپانسیو گوشی/دسکتاپ یک لایه‌ی بهبود به CSS اضافه شد. موارد باقی‌مانده (۸، ۹، ۱۰) جنبه‌ی مستندات/نگهداری دارند و ضرر اجرایی ندارند.

---

## ۱۱) بررسی دور دوم (Round 2) — باگ‌های طراحی و موبایل

در دور دوم، کل HTML/CSS/JS صفحه‌به‌صفحه ممیزی شد (متغیرهای CSS، کلاس‌های بدون استایل، براکت‌های ناقص، ترجمه‌ها، سرریز موبایل). **همه موارد زیر رفع و تست شدند (jsdom روی هر ۱۱ صفحه در هر دو زبان FA/EN بدون خطا):**

### 🔴 باگ‌های بحرانی (قابل‌مشاهده)

| # | باگ | فایل | رفع |
|---|-----|------|-----|
| ۱۱-۱ | دکمه همبرگری منو (`menu-toggle`) هیچ استایلی نداشت → در موبایل نامرئی بود و منو **اصلاً باز نمی‌شد** | `css/style.css` | استایل کامل دکمه + انیمیشن تبدیل به × هنگام باز بودن |
| ۱۱-۲ | قواعد `.preset-rail` و `.preset` در `home.css` خراب شده بود (سلکتور گم شده بود و دو declaration بی‌صاحب مانده بود) → کل بخش «نسخه‌ها / Presets» بدون استایل | `css/home.css` | بازسازی کامل rail + کارت + نشان «پیشنهادی RX 580» |
| ۱۱-۳ | متغیرهای `--container`، `--gutter`، `--overlay` هیچ‌جا تعریف نشده بودند → عرض صفحه‌ها در دسکتاپ بی‌نهایت (full-bleed)، پدینگ سایدبار ۰ و overlay شفاف | `css/style.css` | تعریف در `:root` (container: 1280px) |
| ۱۱-۴ | متغیرهای تم تاریک گم‌شده: `--sidebar-bg`، `--toast-bg`، `--shadow-soft`، `--accent-soft-2`، `--line-red` → توست شفاف، سایدبار شفاف، پس‌زمینه hover/warn/alert از بین رفته | `css/style.css` | تعریف در بلوک dark |
| ۱۱-۵ | `.sticky-dock.is-on` وجود نداشت → داک چسبان دانلود **هیچ‌وقت ظاهر نمی‌شد** | `css/home.css` | اضافه شدن state نمایش + فاصله بدنه |
| ۱۱-۶ | `.icon-moon` / `.icon-sun` قانون نمایش نداشتند → هر دو آیکون ماه و خورشید همیشه کنار هم دیده می‌شدند | `css/style.css` | نمایش فقط آیکون متناسب با تم |
| ۱۱-۷ | کلاس‌های بخش «شروع سریع» (`.step-grid`، `.step-card`، `.step-top`، `.step-n`، `.step-line`) هیچ استایلی نداشتند | `css/home.css` | استایل کامل کارت‌های ۳ مرحله |
| ۱۱-۸ | `.checklist-progress` / `.checklist-bar` / `.checklist-hint` / `.is-done` بدون استایل → شمارنده و نوار پیشرفت چک‌لیست دیده نمی‌شد | `css/home.css` | استایل کامل |
| ۱۱-۹ | `.brand` / `.brand-logo` / `.brand-text` استایل پایه نداشتند → لوگو و نام برند در هدر درست کنار هم نمی‌نشستند | `css/style.css` | استایل کامل برند |
| ۱۱-۱۰ | یک `}` اضافه در انتهای مدیاکوئری هدر (سینتکس خراب) | `css/style.css` | حذف شد |
| ۱۱-۱۱ | `.hx-cta` قاعده پایه نداشت → دکمه‌های هیرو در دسکتاپ بدون چیدمان flex | `css/home.css` | اضافه شد |

### 🟠 باگ‌های طراحی/سازگاری

| # | باگ | رفع |
|---|-----|-----|
| ۱۱-۱۲ | صفحات آموزش فونت **Lalezar** را لود نمی‌کردند ولی h2 ها (`step-title`) با `--font-fa-display` (Lalezar) رندر می‌شدند → فونت جایگزین زشت | افزودن `family=Lalezar` به لینک فونت هر ۹ صفحه |
| ۱۱-۱۳ | متن «این منوی چپ» در سایدبار و «منوی چپ» در هوم، در حالی که منو در RTL از **راست** باز می‌شد | اصلاح متن به «منو / side menu» |
| ۱۱-۱۴ | کلاس‌های تزئینی بدون استایل: `.dl-glow`، `.creator-glow`، `.dl-text`، `.dl-note`، `.media-chip`، `.ggo`، `.gcard-hot`، `.hx-ver`، `.hx-dot`، `.hx-div`، `.btnx-shine`، `.wrd`/`.wrd-i`، `.en`، `.video-overlay-bg` | همگی استایل گرفتند (شاین دکمه، انیمیشن کلمه‌به‌کلمه هیرو، گلوها و…) |

### 🟢 بهینه‌سازی موبایل (چیزی از صفحه بیرون نمی‌زند)

- `.preset-rail` در موبایل → ریل اسکرول افقی با snap (طبق مستند طراحی)
- `.hx-topline` و `.hx-metrics` → wrap و فاصله‌های کوچک‌تر در ۳۲۰–۴۰۰px
- `.dl-actions` / `.hx-cta` → دکمه‌های بلند (`uhm-graphics-pack.rar`) دیگر بیرون نمی‌زنند (wrap + اندازه کوچک‌تر در ≤480px)
- نوار کنترل ویدیو در ≤480px فشرده شد (دکمه‌ها و تایم کوچک‌تر)
- `.hs-head` در موبایل ستونی می‌شود؛ `.bento-proof` فاصله‌های موبایل
- `viewport-fit=cover` + safe-area برای آیفون‌های ناچ‌دار (هدر/توست/داک/توپ)
- `overflow-x: clip` روی `html` هم اعمال شد (لایه دوم محافظت)

### 🟡 اصلاحات جاوااسکریپت

| # | باگ | رفع |
|---|-----|-----|
| ۱۱-۱۵ | `setLocalized` با `String.replace` اولِ هر کلیدواژه کار می‌کرد؛ اگر دو chip هم‌نام در یک پاراگراف بود مارکاپ خراب می‌شد | بازنویسی با marker یکتا + پردازش بلندترین کلیدواژه اول (تست شده) |
| ۱۱-۱۶ | `video-player.js` روی ویدیوی native صفحه `pure-install` هم سوار می‌شد (تداخل کنترل‌ها) | گارد `hasAttribute("controls")` → پلیر سفارشی فقط روی هوم |
| ۱۱-۱۷ | کلاس‌های `hx-video-*` (ویدیوی هیرو) — ویدیوی هیرو در HTML نیست؛ کد بی‌ضرر است و دست نخورد | — |

**نتیجه تست نهایی:** هر ۱۱ صفحه × ۲ زبان بدون خطای JS؛ chip/strong/em بعد از چرخه FA→EN→FA حفظ می‌شوند؛ هدر/سایدبار/صفحه‌بندی/داک/اسکرول‌پروگرس همه ساخته می‌شوند؛ هیچ متغیر CSS بی‌تعریف و هیچ کلاس بی‌استایلی باقی نمانده است.

---

## ۱) باگ تأییدشده با تست (بالاترین اهمیت)

### 🔴 باگ ۱ — قالب‌بندی (chip / bold) از همه صفحات آموزش حذف می‌شود
**فایل:** `js/app.js` → تابع `applyLang` (خطوط ۱۲۹ و ۱۳۳)

درست وقتی صفحه لود می‌شود، `applyLang()` به‌صورت بی‌قیدوشرط اجرا می‌شود و برای **هر** عنصرِ دارای `data-fa` این کار را می‌کند:

```js
el.textContent = isFa ? fa : en;   // جایگزینی متن به‌جای innerHTML
```

یعنی کل محتوای داخلی عنصر (شامل `<strong>`، `<span class="chip">` و …) نابود می‌شود و فقط متن خام `data-fa` می‌ماند.

در HTML صفحات، چیزهایی مثل این هست:

```html
<p data-fa="تیک Active را برای Extension و second camera روشن کنید و …">
  تیک <span class="chip">Active</span> را برای … و <strong>Script</strong> هر دو را روی
  <span class="chip">KirbyCam</span> بگذارید.
</p>
```

بعد از لود، به‌جای آن فقط متن خام می‌ماند و **هیچ chip یا bold دیده نمی‌شود**.

**نتیجه تست (هر ۱۰ صفحه):** تعداد عناصر `<strong>` و `.chip` داخل `[data-fa]` بعد از لود = **۰**، در حالی که در HTML خام موجود هستند.

> این باگ در هر لود اتفاق می‌افتد، حتی بدون سوییچ زبان. دلیلش این است که `applyLang` برای ترجمه، `innerHTML` را با `textContent` عوض می‌کند و بعد که دوباره به فارسی برمی‌گردی، محتوای غنی از بین رفته. جالب این‌که خود کد یک مکانیزم درست هم دارد (`data-fa-html` در خط ۱۳۲) که نویسنده برای این عناصر استفاده نکرده است.

---

## ۲) باگ‌های سوییچ زبان

### 🟠 باگ ۲ — صفحه‌بندی «قبلی/بعدی» برای کاربران EN ترجمه نمی‌شود
**فایل:** `js/layout.js` → `mountPageNav` (خطوط ۱۵۶–۱۵۸)

نوار صفحه‌بندی با `mountPageNav()` بعد از اجرای `applyLang()` تزریق می‌شود، بنابراین وقتی کاربر زبان را EN ذخیره کرده باشد، این نوار همچنان فارسی می‌ماند. تازه نام بخش‌ها (`prev.fa` / `next.fa`) هم اصلاً `data-en` ندارند.

**نتیجه تست:** با `uhm-lang=en` محتوای نوار چنین شد:
```
→ قبلی: Video Setting بعدی: HUD ←
```
یعنی نه برچسب «قبلی/بعدی» به انگلیسی برگشته و نه نام بخش‌ها.

### 🟡 باگ ۳ — تگ `<html>` همیشه `fa/rtl` می‌ماند
**فایل:** `js/app.js` → `applyLang`

سوییچ زبان فقط `body` را تغییر می‌دهد (بادی‌هول lang/dir) ولی `<html lang="fa" dir="rtl">` دست‌نخورده می‌ماند. خواننده‌های صفحه‌ای و بعضی CSS به `html[lang]`/`html[dir]` تکیه می‌کنند، پس ناسازگار است. (بادی درست عوض می‌شود — تست تأیید کرد.)

---

## ۳) کد مرده / بدون کاربرد

### 🟡 باگ ۴ — کلاس `is-playing` بدون هیچ قانون CSS
**فایل:** `js/video-player.js` خط ۳۳ — کلاس `is-playing` روی فریم عوض می‌شود ولی در هیچ‌جای CSS قانونی ندارد (بر خلاف `is-paused` که دارد). خروجی قابل‌مشاهده‌ای ندارد ولی کد اضافه است.

### 🟡 باگ ۵ — `js/gallery-manifest.js` هرگز لود نمی‌شود (کاملاً مرده)
این فایل `window.UHM_GALLERY` را تعریف می‌کند ولی در هیچ HTML صدا زده نمی‌شود و جایی استفاده نمی‌شود.

### 🟡 باگ ۶ — بلوک گالری خودکار در `app.js` مرده است
کدِ مربوط به `data-autoload`/`data-images` (خطوط ~۲۱۰–۲۳۸) هیچگاه اجرا نمی‌شود، چون صفحه `graphics-test.html` گالری را به‌صورت hard-code دارد و از آن ویژگی‌ها استفاده نمی‌کند. گالری واقعی کار می‌کند، اما این کد اضافه و گمراه‌کننده است.

---

## ۴) کارایی و منابع

### 🟠 باگ ۷ — دو ویدیوی جداگانه ۱۱ مگابایتی از یک فایل در صفحه اصلی
**فایل:** `index.html`

دو عنصر `<video>` با همین یک فایل `uhm-showcase.mp4` (حدود ۱۱ مگابایت) وجود دارد:
- ویدیوی پس‌زمینه هیرو (`heroBgVideo`) با autoplay
- پلیر showcase (`showcaseVideo`) با `preload="auto"`

یعنی در یک لندینگ ساده، حدود **۲۲ مگابایت ویدیو** لود/دیکد می‌شود. برای سایتی که هدفش دانلود یک پک ~۴.۴MB است، سنگین است. بهتر است یکی از این‌ها lazy باشد یا از یک فایل، استفاده شود.

### 🟡 باگ ۸ — ویدیوی لوکال آموزش Pure وجود ندارد
`pages/pure-install.html` اول به فایل لوکال `../assets/media/pure-install-tutorial.mp4` اشاره می‌کند که وجود ندارد (در `assets/media/README.txt` هم نوشته شده اختیاری است) و فقط به CDN وابسته است. اگر CDN در دسترس نباشد، ویدیو پخش نمی‌شود.

---

## ۵) ناسازگاری و نگهداری

### 🟡 باگ ۹ — محتوای تکراری
فایل‌های `نصب (…).txt` هم در ریشه و هم در پوشه `content/` وجود دارند؛ اما هیچ‌کدام در سایت استفاده نمی‌شوند (محتوا hard-code در HTML است). خطر ویرایش اشتباه: کسی فکر کند با تغییر این فایل‌ها سایت عوض می‌شود.

### 🟡 باگ ۱۰ — دو پوشه متفاوت برای گالری
- پوشه `assets/graphics-test/` خالی است (فقط README و .gitkeep)
- عکس‌های واقعی در `assets/guides/09-graphics-test/` هستند

در README و کد، مسیر `assets/graphics-test` آمده که گمراه‌کننده است.

---

## ۶) چیزهایی که بررسی شد و سالم بود ✅

- همه مسیرهای asset (عکس، ویدیو، CSS، JS) در تمام صفحات درست resolve می‌شوند (تنها `pure-install-tutorial.mp4` لوکال عمداً غایب است + CDN fallback دارد).
- فایل دانلود `uhm-graphics-pack.rar` موجود، معتبر (RAR5) و ~۴.۴MB است.
- سینتکس همه فایل‌های JS سالم است (`node --check`).
- هدر/سایدبار با `layout.js` درست ساخته می‌شود و هیچ `ReferenceError` واقعی وجود ندارد.
- سوییچ زبان (FA↔EN) روی بادی کار می‌کند و متن‌ها ترجمه می‌شوند.
- سوییچ تم dark/light کار می‌کند.
- گالری تست گرافیک و لایت‌باکس، دکمه کپی مسیر، شمارنده‌ها، چک‌لیست و ناوبری بین ۱۰ صفحه سالم هستند.
- شماره‌بندی ۱→۱۰ صفحات با منو و کارت‌ها هماهنگ است.

---

## ۷) پیشنهاد اولویت رفع

| اولویت | مورد |
|--------|------|
| **بالا** | باگ ۱: حفظ قالب‌بندی هنگام ترجمه (استفاده از `data-fa-html` یا فقط عوض کردن متن بدون `textContent` روی عناصر دارای فرزند) |
| متوسط | باگ ۲ و ۳: ترجمه صفحه‌بندی + عوض کردن `<html lang/dir>` |
| متوسط | باگ ۷: کاهش لود ویدیوی تکراری (lazy / پستر) |
| پایین | باگ‌های ۴، ۵، ۶ (کد مرده) و ۸، ۹، ۱۰ (ناسازگاری) |

---

## ۸) جمع‌بندی تغییرات انجام‌شده (رفع باگ‌ها)

| فایل | تغییر |
|------|-------|
| `js/app.js` | باگ ۱: تابع `setLocalized` — ترجمه بدون ازبین‌بردن `chip`/`<strong>`/`<em>` (بازسازی با حفظ مارکاپ). باگ ۳: همگام‌سازی `html[lang]/dir`. حذف کد مرده‌ی گالری خودکار (باگ ۶). |
| `js/layout.js` | باگ ۲: صفحه‌بندی «قبلی/بعدی» حالا بر اساس زبان ذخیره‌شده ترجمه می‌شود و نام بخش‌ها هم `data-en` دارند؛ بعد از تزریق هم با زبان همگام می‌شود. |
| `index.html` | باگ ۷: ویدیوی showcase `preload="none"` + پستر → فقط با کلیک لود می‌شود (به‌جای لود خودکار ~۱۱MB اضافه). |
| `js/video-player.js` | لود ویدیو قبل از اولین پخش (چون `preload="none"` است). |
| `css/style.css` | باگ ۴: قانون واقعی برای کلاس `is-playing`. |
| `js/gallery-manifest.js` | باگ ۵: فایل مرده حذف شد. |
| `css/home.css` | ریسپانسیو: لایه‌ی بهبود لمسی (تچ)، محافظت لمسی دکمه‌ها، و مرتب‌سازی شناورهای هیرو در صفحه‌های خیلی کوچک. |

تمام تغییرات با اجرای صفحه‌ها در مرورگر آزمایشگاهی تست شدند: مارکاپ در هر ۱۰ صفحه حفظ می‌شود، سوییچ EN→FA مارکاپ را نمی‌شکند، صفحه‌بندی و `html[lang/dir]` در زبان EN درست است و هیچ خطای JS روی هیچ صفحه‌ای نیست.

اگر خواستید می‌توانم بقیه‌ی موارد نگهداری (۸، ۹، ۱۰) را هم تمیز کنم یا این تغییرات را commit کنم.
